# Three Ms Framework

Adapted from Nate Herkai's AIS-OS one-pager (`archives/the-3ms-framework.html` if mirrored, else see `Support Information/Nate Support/the-3ms-framework.html`). Solomon uses this as a navigation lens, not as a folder name.

## Mindset

The operator brain. In Solomon: `foundation/01-belief-system.yaml` + `02-why.yaml` + `03-principles.yaml`. Practiced via the Default Shift ritual ("to what extent could AI be leveraged here?").

## Method

The decision-making discipline. In Solomon: the §2.2.5 pipeline (Capture → Salience → Classification → Hard-rule → Retrieval → System 1 → System 2 → Audit → Owner-state → Action), `decisions/log.md`, and `references/system1-system2.md`.

## Machine

The execution surface. In Solomon: the orchestrator runtime, the 27 skills, the 6 Hermes plugins, the 3 workers, the 17 SQL tables, Pinecone, the 12 sleep-cycle jobs.

The Three Ms install via repetition. Solomon's `solomon-level-up` skill walks the 3Ms interview weekly.
