# 06-taxonomy.yaml — derived summary; canonical store is db/schemas/captured_items.sql.
# Topics: project categories / people categories / time categories / risk scale / decision scale / industry terms / internal language / health metrics
# Filled by solomon-onboarding-06-taxonomy or by intake.md -> solomon-extraction.
# Sleep-Cycle Job 12 (yaml-reconcile) catches drift; DB-wins per §1.

last_updated: null

# (placeholder — filled at end-of-onboarding from captured_items rows where domain matches this YAML)
raw_notes: |
